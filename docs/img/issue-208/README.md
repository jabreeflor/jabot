# Settings can rerun onboarding (#208)

Live evidence from `./scripts/live.sh` against a real `jabot-hostd`. The
Settings pane scrolls inside `.page-scroll`, so the Setup shot was scrolled
to the control before capture.

| file | what it shows |
| --- | --- |
| `settings-setup.png` | Settings → General, scrolled to **Setup** and **Run setup again** |
| `setup-from-settings.png` | The same first-run name pane, seeded with the stored name |
| `setup-escape.png` | Escape left setup and returned to the shell; the name is still Jabree Flor |
| `walkthrough.html` | Mechanism explainer for the PR artifact |
