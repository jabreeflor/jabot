# PR #288 — Windows Inbox toasts

Walkthrough for the #284 notify backend split. The PNG is a Playwright
render of `walkthrough.html` (the mechanism, not a Windows Action Center
capture — this agent ran on Linux).
