# Issue #223 evidence

Real Vite renderer connected to this worktree’s `jabot-hostd`. Aider is a
catalog preset, so New Chat and Settings list it without a separate UI path.

| file | what it shows |
| --- | --- |
| `new-chat-picker.png` | New Chat harness picker with Aider and its “not native ACP” blurb |
| `settings-harnesses.png` | Settings enable/disable list including Aider |

Captured with `./scripts/live.sh shot` against the live host, not a mock.
