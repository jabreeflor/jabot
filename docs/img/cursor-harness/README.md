# Cursor Agent harness — UI evidence

Real Vite renderer + `jabot-hostd` via `./scripts/live.sh`. Captured on
this worktree after the host catalog listed `cursor`.

| file | what it shows |
| --- | --- |
| `new-chat-picker.png` | New Chat with **Cursor Agent** selected as the thread harness. The Doctor install hint is under the composer because the CLI is not on this machine. |
| `settings-enable.png` | Settings → Harnesses. Cursor Agent is enabled, with the install hint and the account-isolation note (`CURSOR_API_KEY` — not isolated per bot). |
| `onboarding-engine.png` | First-run engine pane. Cursor Agent is a card next to the other catalog engines, including its install hint. |

No `--force` / `--yolo` appears on any of these surfaces. Permissions stay in JaBot.
