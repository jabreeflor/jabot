# New Chat window

Screenshots of the Vite app at `http://localhost:1420` (Chromium, deviceScaleFactor 2). The browser preview has no host, so the host chip reads offline and the workspace pills stay disabled — those work in the desktop app.

| file | what it shows |
| --- | --- |
| `empty.png` | New Chat as a main-pane conversation: context chips, large composer, workspace pills |
| `harness.png` | Harness list opened from the chip in the context row |
| `prompt.png` | A first message typed; the mic becomes a send arrow |
| `folder.png` | Starting from a folder's ＋ pre-selects that workspace |
