# Issue #183 evidence

Real Vite renderer connected to this worktree’s jabot-hostd. A test-only ACP process deliberately sends an empty successful reply; the real host and renderer surface the error.
